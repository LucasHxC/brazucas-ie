import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'brazucas.ie',
  appName: 'brazucas-ie',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
